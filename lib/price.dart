import 'package:flutter/material.dart';
import 'package:nested_tab_ui/make_offer.dart';

class Price extends StatelessWidget {
  String text;
  Price({this.text});
  MakeOffer _makeOffer = MakeOffer();
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
